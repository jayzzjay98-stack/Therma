# 🔐 Security Report — Therma (senior-security skill)

**Generated:** 2026-03-24  
**Tools:** secret_scanner.py + threat_modeler.py  
**Scope:** Sources/, scripts/, shell scripts, update pipeline

---

## ✅ Secret Scan — CLEAN

| Target | Secrets Found |
|--------|--------------|
| `Sources/` (40 Swift files) | **0** ✅ |
| `scripts/` | **0** ✅ |

> **Note:** `.build/` directory was excluded — debug symbols (`.dSYM`) generate thousands of false positives with pattern-matching scanners. Source code is clean.

---

## 🧩 Threat Model — UpdateManager (GitHub Auto-Updater)

> **Highest-risk component:** Downloads, verifies, and installs app updates from GitHub releases via shell script.

| Risk | Threat | Category | Score | Relevant to Therma? |
|------|--------|----------|-------|---------------------|
| 🔴 Critical | Supply chain attack via tampered `.zip` | Tampering | 20 | ✅ Yes — mitigated by `codesign --verify --deep --strict` |
| 🔴 Critical | Untrusted download URL redirect | Spoofing | 20 | ✅ Mitigated — URL validated: `https://github.com` or `githubusercontent.com` only |
| 🟠 High | Shell script injection via filename | Tampering | 15 | ✅ Mitigated — paths are `shellEscape()`'d |
| 🟠 High | Man-in-the-middle on download | Information Disclosure | 12 | ✅ HTTPS-only enforced via `url.scheme == "https"` |
| 🟡 Medium | Quarantine bypass leaves malware | Tampering | 8 | ⚠️ Needs review — `xattr -rd quarantine` is intentional but must be post-codesign only |

**Overall security posture: Good** — all critical mitigations are implemented in code.

---

## 🧩 Threat Model — RAMMonitor + PurgeManager

> Reads kernel memory stats and runs privileged `purge` commands.

| Risk | Threat | Category | Score | Relevant to Therma? |
|------|--------|----------|-------|---------------------|
| 🟠 High | Privilege escalation via purge command | Elevation of Privilege | 16 | ⚠️ `purge` runs via shell — consider sandboxing |
| 🟠 High | Memory data exposure to other processes | Information Disclosure | 12 | ✅ macOS sandbox handles this |
| 🟡 Medium | DoS via repeated deep clean triggers | Denial of Service | 9 | ✅ Mitigated — `cleanTimeoutSeconds = 25` + UI lock |

---

## ⚠️ Action Items (Real Issues Found)

### Issue 1 — Quarantine Bypass Ordering (Low-Medium Risk)
**File:** `UpdateManager.swift` line ~147  
```swift
try? await run("/usr/bin/xattr", ["-rd", "com.apple.quarantine", newApp.path])
// ↑ This runs BEFORE codesign verification in the catch block
```
The quarantine attribute removal happens before signature verification in the fallback path. Consider ensuring `verifySignature` always runs *before* removing quarantine.

### Issue 2 — GitHub API URL is Hardcoded (Low Risk)
**File:** `UpdateManager.swift` line 30  
```swift
private let apiURL = URL(string: "https://api.github.com/repos/jayzzjay98-stack/Therma/releases/latest")!
```
The repo path is hardcoded with a comment saying `// ✏️ Change to your actual GitHub repo path`. This is fine for production but should be updated before distributing to other users/forks.

---

## ✅ Security Strengths

| Area | Status |
|------|--------|
| No hardcoded API keys or tokens | ✅ Clean |
| HTTPS-only download enforcement | ✅ |
| Host allowlist for downloads | ✅ `github.com` / `githubusercontent.com` only |
| Shell path escaping | ✅ `shellEscape()` implemented |
| Codesign verification before install | ✅ `codesign --verify --deep --strict` |
| Bundle integrity validation | ✅ `bundleLooksValid()` checks `Info.plist` + executable |
| `.DS_Store` / `._` cleanup | ✅ `sanitizeExtractedBundle()` |
| Download progress timeout | ✅ `cleanTimeoutSeconds = 25` |

---

*Generated by senior-security skill — Antigravity AI*
